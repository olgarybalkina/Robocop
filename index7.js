const searchName = (arr, searchElement) => {
   if (!Array.isArray(arr) || typeof searchElement !== 'string') return;

   const searchOfValue = arr.find((searchElement, index, arr) => {
      if (name === searchElement)
      return arr.splice
   });